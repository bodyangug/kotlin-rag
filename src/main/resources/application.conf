ktor {
    deployment {
        port = 8080
        port = ${?PORT}
        host =  "0.0.0.0"
    }
    application {
        modules = [ com.pandus.llm.rag.RagKotlinApplicationKt.module ]
    }
}

myapp {
    llm {
        model {
            temperature = 0.2
            max_tokens = 500
        }
        chat_memory {
            max_messages = 10
        }
        tokenizer {
            max_segment_size_in_tokens = 1000
            max_overlap_size_in_tokens = 200
        }
        embedding {
            min_score = 0.75
        }
    }
    azure {
        endpoint = "https://chatbot-pandus.openai.azure.com"
        api_key = "139cd87834bb4c18834a1e9070ace419"
        deployment_name = "gpt-4o"
        tokenizer_name = "text-embedding-ada-002"
    }
    chroma {
        image_name = "chromadb/chroma:0.5.4"
    }
}
